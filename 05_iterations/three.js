//Higher order loops

//for of

const arr = [1, 2, 3, 4, 5]
for(const num of arr){
    // console.log(num);
}

//Map - An object that holds key, value pairs
const mp = new Map()
mp.set('IN', "India")
mp.set('USA', "United states of America")

console.log(mp);


// the for of loop can not iterate over the objects. so that for in is used for objects